    let matrix =[];
    for (let x=0; x<5;x++){
        matrix[x] = new Array();
        
        for(let y=0; y<5; y++){
            switch(y)
          
    }}
    console.table(matrix);

let filas=[];
let columnas=[];
let fila=Number(prompt("cuantas filas desea"))
let columna=Number(prompt("cuantas columnas desea"))

for(let i=0; i<fila ; i++){
    filas[i]= columnas[i]
    for( let j=0; j<columna ; j++){
        columnas[j]= Number(prompt("ingrese el numero"));
    } }
console.table(filas);
