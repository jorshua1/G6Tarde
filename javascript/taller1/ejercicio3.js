let base = parseInt(prompt("Ingrese el valor de la base"))
let altura = parseInt(prompt("Ingrese el valor de la altura"))

let area = (base*altura)/2;
console.log("EL resultado del area del triangulo es: " +area);
