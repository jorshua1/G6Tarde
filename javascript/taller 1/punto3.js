// punto 3
let base=parseInt(prompt("ingrese el valor la base del triangulo"));
let altura=parseInt(prompt("ingrese el valor de la altura del triangulo)"));
let area=(base*altura)/2;
console.log(area);