let base = parseInt(prompt("ingrese la base del triagulo "))
let altura = parseInt(prompt("ingrese la altura del triangulo)"))

let operacion = base * altura / 2 

console.log("el area del triangulo es de" + operacion)
